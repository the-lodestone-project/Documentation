# custom_code

`custom_code` is a new addition to the Mineflayer.py package that allows you to run custom code on the bot.



```python filename="custom_code.py" {3} copy
# if no `server` is specified the current server will be used
bot.custom_code(code="bot.bot.players")
```